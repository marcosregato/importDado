CREATE TABLE TURMA (
    id INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
    ALUNO VARCHAR(50),
    PERIODO VARCHAR(50),
    MATRICULA DECIMAL(10,2)
);

CREATE TABLE PROFESSOR (
    id INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
    NOME VARCHAR(50),
    MATERIA VARCHAR(50)
);

